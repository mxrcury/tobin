import TodoListContainer from "./components/Container/TodoListContainer";

const App = (props) => {
  return (
    <div className="app-container">
      <TodoListContainer />
    </div>
  );
};

export default App;
